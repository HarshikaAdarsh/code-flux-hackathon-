# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Config / validation ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Database ---
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pgvector==0.3.6
greenlet==3.1.1

# --- Auth ---
PyJWT==2.10.1
bcrypt==4.2.1

# --- HTTP / AI providers ---
httpx==0.28.1

# --- Syllabus parsing ---
pypdf==5.1.0

# --- Text to speech (free, supports en + hi) ---
# Keep this current: it talks to an unofficial Microsoft endpoint that
# rejects older client versions with a 403. Gemini TTS is the fallback.
edge-tts==7.2.8
